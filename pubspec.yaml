name: multi_border
description: A decoration class for quickly adding multiple borders of varying colors and widths to your widgets.
version: 1.0.0
homepage: https://github.com/benjamin-otto/multi_border

environment:
  sdk: ">=2.19.3 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter: null
